# تقرير إصلاح تقارير المشاريع والطباعة

التاريخ: 2026-07-27

## الوضع السابق

كان قسم تقرير المشاريع في `Reports.jsx` يقرأ:

- مستندات `projects`.
- مجموعات المتطوعين الرئيسية.
- `projects/{projectId}/projectGroups`.

ثم يحول مجموعات المشروع إلى أسماء داخل صف المشروع. لم يكن يقرأ
`elderlyParticipants`، ولم يربط `projectGroups.volunteerIds` بسجلات المتطوعين،
ولم يحسب الإحصاءات من العلاقات الفعلية. لذلك بقي التقرير جدول أسماء وحقول
ملخصة قديمة بدل تقرير محتوى المشروع.

## السبب الجذري لنقص التقرير

نموذج البيانات الفعلي موزع بين:

- `projects/{projectId}`: الاسم، النوع، السنة، تواريخ البدء والتوزيع، الحالة،
  الملاحظات، الأحياء المختارة وحقول المشروع الأخرى.
- `projects/{projectId}/elderlyParticipants/{elderlyId}`: حالة استلام الحزمة،
  حالة التسليم، الملاحظات، الحي، الإسناد إلى مجموعة أو متطوع، ونسخة من بيانات
  الاتصال الأساسية.
- `projects/{projectId}/projectGroups/{groupId}`: علاقة المجموعة بالمشروع
  ومصفوفة `volunteerIds`.
- `elderly`: البيانات الحالية للمسنين.
- `volunteerGroups`: بيانات المجموعة والاتصال.
- `volunteers`: بيانات المتطوعين المرتبطين فعليًا بالمجموعة داخل المشروع.

التقرير السابق لم يجمع هذه العلاقات.

## أسباب تعطل أزرار الطباعة

1. طباعة المسنين داخل حي المشروع كانت تفتح `PrintModal` دون `data` أو
   `columns`، فكانت تطبع جدولًا فارغًا.
2. فلتر الحزمة والتسليم في النافذة نفسها لم يحتوِ `key`، فلا يمكن تطبيقه.
3. طباعة قائمة المشاريع استعملت `projects` كاملة بدل `filtered`، فتجاهلت
   فلاتر الصفحة الحالية.
4. `PrintModal` كان يبني HTML من قيم Firestore ويمرره إلى
   `document.write` دون escaping، ما يجعله معرضًا لـHTML injection/XSS.
5. تقرير المشاريع العام كان يستخدم مصدر PDF الجدولي العام الذي لا يعرف
   المشاركين أو المجموعات ولا يطبع تقرير مشروع مفصل.

## البيانات التي يعرضها التقرير الآن

لكل مشروع:

- الاسم، النوع أو المناسبة المتاحة، السنة.
- تاريخ البدء وتاريخ التوزيع.
- الحالة.
- عدد المسنين، الحزم، المسندين، المسلّمة ونسبة التقدم.
- الأحياء.
- المشاكل أو الملاحظات المتاحة فعليًا.
- المسنون المشاركون: الاسم، الهاتف، العنوان، الحي، الحزمة، التسليم، المجموعة
  المسندة، المتطوع الثابت والملاحظات.
- المجموعات المشاركة: الاسم، جهة الاتصال، الهاتف، عدد وأسماء المتطوعين،
  عدد المسنين والحزم المسندة.

المشروع بلا علاقات يظهر بصورة سليمة مع رسائل “لا توجد بيانات”.

## طريقة الجلب والتجميع والأداء

يتم تحميل ست مجموعات بيانات بالتوازي:

1. المشاريع.
2. كل `elderlyParticipants` التابعة للمشاريع باستعلام collection-group واحد.
3. كل `projectGroups` التابعة للمشاريع باستعلام collection-group واحد.
4. المسنون.
5. مجموعات المتطوعين.
6. المتطوعون.

بعد ذلك يتم الربط في الذاكرة بواسطة `Map` حسب المعرّفات. عدد الاستعلامات ثابت
ولا يزيد مع عدد المشاريع، وبالتالي لا يوجد N+1. المشاركون والمجموعات
والمتطوعون تُزال تكراراتهم بالمعرّف، ولا تنتقل علاقة من مشروع إلى مشروع آخر.

## التغييرات البصرية

- بقي الجدول والفلاتر والمخططات الحالية.
- أضيف قسم “تفصيل كامل حسب المشروع” على هيئة بطاقات قابلة للفتح والإغلاق.
- تفاصيل كل مشروع منظمة في مؤشرات مختصرة ثم جدول مسنين وجدول مجموعات.
- أضيفت أزرار مستقلة لطباعة التقرير الكامل، المسنين فقط، والمجموعات فقط.
- التصميم يستخدم مكونات وألوان الإدارة الحالية ويدعم RTL مع تمرير أفقي
  للجداول الطويلة.

## تعديلات الطباعة وحماية XSS

أضيفت أداة `openSafePrintReport` موحدة:

- لا تستخدم `document.write`.
- تنشئ عناصر DOM صريحة وتضع كل قيمة عبر `textContent`.
- لا تنشئ عناصر أو scripts من قيم Firestore.
- تضبط RTL والعناوين وتاريخ الطباعة وعدد النتائج.
- تكرر رأس الجدول في الصفحات وتحاول منع انقسام الصفوف والأقسام.
- تعمل عند فتح وإغلاق المعاينة أكثر من مرة.

تم تحويل كل `PrintModal` المرتبط بالمشاريع إلى الأداة الآمنة. مسار تقرير
المشاريع في صفحة التقارير يتجاوز مصدر الطباعة العام القديم ويستخدم التقرير
المفصل الآمن. لم تُغيّر طباعة أنواع التقارير الأخرى.

## الملفات المعدلة

- `frontend/src/admin/Reports.jsx`
- `frontend/src/admin/Projects.jsx`
- `frontend/src/utils/projectReportModel.js`
- `frontend/src/utils/safePrint.js`
- `frontend/tests/project-reports.test.mjs`
- `frontend/package.json`
- `PROJECT-REPORTS-AND-PRINT-FIX-REPORT.md`

## الاختبارات المنفذة

- `npm.cmd run build`: نجح.
- `npm.cmd run test:project-reports`: نجحت 7 اختبارات تغطي:
  - الربط الصحيح للمسنين والمجموعات والمتطوعين.
  - منع التكرار وتسرب بيانات مشروع آخر.
  - مشروع فارغ وعلاقات ناقصة.
  - مشروع بمسنين فقط أو مجموعات فقط.
  - 1200 مشارك.
  - الفلاتر الموسعة.
  - XSS والطباعة المتكررة ومسارات أزرار الطباعة.
- `npm.cmd run test:db01`: نجح، 6 unit واختبار Emulator.
- `npm.cmd run test:db04`: نجح، 5 unit و80 حالة/214 فحص Emulator.
- `npm.cmd run test:scalability`: نجح، 4 unit و1001 كتابة/حذف داخل Emulator.
- `npm.cmd run test:reliability`: نجح، unit وReact وFirebase Emulator.
- `npm.cmd run test:smoke`: نجح على build المحلي.

كل اختبارات Firebase استخدمت مشاريع `demo-*` داخل Emulator. لم تحدث كتابة
على Production.

## نتائج الفحص اليدوي

- تم تشغيل build محلي ومعاينته على `127.0.0.1`.
- route `/admin/reports` عمل وأعاد واجهة التطبيق.
- الوصول إلى التقرير الفعلي توقف بصورة صحيحة عند صفحة تسجيل الدخول لأن
  المتصفح المحلي لا يملك جلسة مدير.
- لم تُستخدم بيانات اعتماد ولم يتم تجاوز المصادقة.

لذلك تم التحقق من سيناريوهات مشروع بمسنين ومجموعات، مسنين فقط، مجموعات فقط،
مشروع فارغ، الفلاتر، الطباعة الكاملة، طباعة مشروع واحد، وطباعة القوائم عبر
اختبارات النموذج والطباعة المعزولة. لم يمكن تأكيد المظهر النهائي داخل جلسة
إدارة فعلية أو فتح native print preview بصريًا دون حساب مدير.

## المخاطر والقيود المتبقية

- تحميل بيانات المسنين والمتطوعين كاملة يتم مرة واحدة لتجنب N+1؛ هذا مناسب
  للبنية الحالية، لكنه قد يحتاج API تقارير خادميًا إذا أصبحت المجموعات ضخمة
  جدًا مستقبلًا.
- العلاقات الناقصة تعرض البيانات المتاحة ولا تنهار، لكن لا يمكن إظهار اسم
  سجل رئيسي محذوف.
- يلزم فحص بصري أخير بحساب مدير على بيانات فعلية قبل النشر.
- لم تُغيّر Firestore Rules أو نموذج البيانات، ولا توجد Migration.
- لم يُنفذ Deploy.

## الحكم النهائي

**تحتاج بيانات فعلية للاختبار.**

الإصلاح البرمجي والاختبارات مكتملان، لكن الحكم “محلولة بالكامل” يحتاج فحص
واجهة وطباعة بصريًا داخل جلسة مدير تحتوي بيانات المشاريع الفعلية.
