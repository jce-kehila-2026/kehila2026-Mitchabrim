# PROJECT REPORTS — GROUP FILTER AND PRINT

## الحكم التنفيذي

**الحكم النهائي: تحتاج بيانات فعلية إضافية للاختبار.**

اكتمل التنفيذ البرمجي والاختبارات الآلية المطلوبة دون تغيير نموذج Firestore أو Rules ودون Deploy أو كتابة على Production. المتبقي هو تحقق بصري داخل جلسة إدارة مصرح بها باستخدام بيانات فعلية تحتوي إسنادات مجموعات وإسنادات فردية.

## الوضع السابق

- كان رسم تقرير المشاريع يعتمد على ملخص المشروع، ولم يكن ترتيب التاريخ يتعامل بوضوح مع الحقول الزمنية المختلفة أو التاريخ الناقص.
- كان فلتر المجموعة يرشح المشاريع بحسب أسماء المجموعات، لكنه لا يضيّق قائمة المواطنين داخل المشروع إلى المواطنين المسندين فعليًا للمجموعة المختارة.
- لم يكن `עצמאיים` ممثلًا كخيار مجموعة في تقرير المشاريع.
- لم تكن المسؤولية الفردية ظاهرة في التقرير الشامل.
- كانت بعض أزرار الطباعة القديمة في قسم المشاريع تستخدم نافذة طباعة غير آمنة؛ أصلحها التعديل السابق، وهذا التعديل يحافظ على المسار الآمن ويجعل بياناته مطابقة لفلتر المشروع والمجموعة.

## نموذج البيانات الفعلي

- بيانات المشروع الأساسية: `projects/{projectId}`.
- المشاركون: `projects/{projectId}/elderlyParticipants/{elderlyId}`.
- وثيقة المشارك تحتوي فعليًا على:
  - `receives`: هل المواطن مخصص له حزمة.
  - `delivery`: حالة التسليم.
  - `assignedGroupId`: المجموعة المسؤولة داخل المشروع.
  - `assignedVolunteerId` و`assignedVolunteerName`: المتطوع المستقل المسؤول.
  - `notes`: الملاحظات.
- المجموعات المشاركة: `projects/{projectId}/projectGroups/{groupId}`، وبداخلها `volunteerIds`.
- البيانات المرجعية: `elderly` و`volunteerGroups` و`volunteers`.

## تحديد استلام الحزمة

يُحسب المواطن كمستلم فقط عندما تكون قيمة `delivery` مساوية حرفيًا لـ`נמסר`. وجود المواطن في المشروع أو كون `receives === "כן"` لا يُعد إثباتًا للتسليم.  
`לא נמסרו` يحسب المشاركين الذين لديهم حالة تسليم مسجلة وليست `נמסר`. السجلات التي لا تحتوي حالة تسليم لا تُصنف افتراضيًا، منعًا لاختراع معنى غير موجود.

## ترتيب المشاريع في الرسم

يُرتب الرسم من الأقدم إلى الأحدث باستخدام أول قيمة متاحة بالترتيب:

1. `date`
2. `startDate`
3. `distributionDate`
4. `year` كبداية السنة

تُدعم قيم Firestore Timestamp والقيم النصية القابلة للتحويل إلى تاريخ. المشاريع التي لا تحتوي أي تاريخ أو سنة توضع في نهاية الرسم، مع ترتيب ثابت بالاسم.

محور X يعرض أسماء المشاريع، ومحور Y يعرض عدد المواطنين الذين حالتهم `delivery === "נמסר"`. أضيف تدوير بسيط لأسماء محور X لتقليل التزاحم.

## ربط المواطن بالمجموعة

- عند وجود `assignedGroupId`، تتم المطابقة مع معرّف المجموعة، وليس اسمها، لمنع الالتباس بين مجموعات متشابهة الأسماء.
- تؤخذ تسمية المجموعة من `volunteerGroups` أو من مجموعة المشروع الفعلية.
- حتى إن كان سجل `projectGroups` ناقصًا، يبقى `assignedGroupId` في وثيقة المواطن علاقة مباشرة قابلة للإثبات، وتُستخدم إن كانت المجموعة المرجعية موجودة.
- عند تطبيق فلتر المجموعة، لا تظهر إلا وثائق المشاركين التي تحمل `assignedGroupId` المطابق.
- لا تُنسب السجلات غير المسندة إلى أي مجموعة.

## تمثيل `עצמאיים`

يظهر خيار `עצמאיים` فقط للمشاريع التي تحتوي مشاركًا لديه `assignedVolunteerId` فعلي. عند اختياره، تظهر فقط سجلات المواطنين المسندة إلى متطوع مستقل. يظهر اسم المتطوع من `assignedVolunteerName` أو من سجل `volunteers` المرتبط بالمعرّف.

لا تُعامل العضوية العامة لمتطوع غير تابع لمجموعة كإثبات أنه وزع لمواطن؛ الدليل المعتمد هو الإسناد الموجود على وثيقة المشارك.

## الفلاتر والتسميات

- تسمية `חג` أصبحت `פרויקט` في حقول تقرير المشاريع.
- تسمية العدد هي `אזרחים ותיקים` في الواجهة والجدول والرسم والطباعة.
- فلتر المشروع يعمل مع فلتر المجموعة في الوقت نفسه.
- خيارات المجموعة تستخدم قيمة داخلية مبنية على المعرّف وتعرض الاسم للمستخدم.
- النتائج والإحصاءات والتفاصيل والطباعة يعاد حسابها من المواطنين المطابقين للفلتر.

## التقرير الشامل

يعرض التقرير:

- الاسم والنوع أو المناسبة والتاريخ والسنة والحالة.
- عدد المواطنين، الحزم، من استلم، ومن لديه حالة مسجلة ولم يستلم.
- التقدم وعدد المسندين.
- المجموعات والمستقلين.
- بيانات المواطنين وحالة الحزمة والتسليم.
- المجموعة أو الجهة المسؤولة، واسم المتطوع المستقل عند وجوده.
- المتطوع الدائم والملاحظات والمشاكل والبيانات المرجعية المتاحة.

## الطباعة

- طباعة قائمة المشاريع تستخدم النتائج الحالية بعد الفلاتر.
- طباعة تقرير مشروع واحد وقائمة مواطنيه ومجموعاته تستخدم النموذج المجمع الحالي.
- اختيار مشروع ومجموعة أو `עצמאיים` يضيّق أقسام الطباعة إلى المشاركين المطابقين فقط.
- أزرار `הדפסת רשימה` في قسم المشاريع تستخدم الطابعة الموحدة الآمنة.
- الطباعة RTL، ولا تطبع عناصر التحكم، وتضيف العنوان وتاريخ الطباعة وعدد النتائج.
- لا يتم تمرير قيم Firestore إلى `document.write`.

## حماية XSS

`openSafePrintReport` ينشئ عناصر DOM ويضع كل القيم الديناميكية عبر `textContent`. لا يتم تفسير `<script>` أو السمات الخبيثة أو HTML القادم من Firestore. وجود `document.write` في مسارات تقارير أخرى قديمة خارج نطاق تقرير المشاريع لا يُستخدم في أي مسار طباعة مشاريع عدّله هذا العمل.

## الأداء ومنع التكرار

- جلب المشاريع والمشاركين ومجموعات المشاريع والمسنين والمجموعات والمتطوعين يتم في عدد ثابت من القراءات المتوازية، وليس استعلامًا لكل مشروع.
- التجميع يتم عبر `Map` حسب المعرّفات.
- المشاركون والمجموعات و`volunteerIds` تزال تكراراتهم قبل العرض.
- تغيير الفلتر محلي ولا يعيد جلب بيانات Firestore.
- لا تنتقل بيانات مشارك من مشروع إلى آخر.

## الملفات المعدلة

- `frontend/src/admin/Reports.jsx`
- `frontend/src/admin/Projects.jsx`
- `frontend/src/utils/projectReportModel.js`
- `frontend/src/utils/safePrint.js`
- `frontend/tests/project-reports.test.mjs`
- `frontend/package.json`
- `PROJECT-REPORTS-GROUP-FILTER-PRINT-REPORT.md`

## الاختبارات والنتائج

- `npm.cmd run test:project-reports`: **نجح، 9/9**.
  - التجميع ومنع التكرار والتسرب بين المشاريع.
  - حساب المستلمين وغير المستلمين.
  - ترتيب الرسم زمنيًا والتاريخ الناقص.
  - مشروع فارغ وعلاقات ناقصة.
  - أكثر من مجموعة، ومجموعة محددة، و`עצמאיים`.
  - الجمع الكبير (1200 مشارك).
  - فلتر مشروع + مجموعة.
  - طباعة آمنة، XSS، وفتح نافذة الطباعة أكثر من مرة.
  - تحقق ساكن من التسميات ومسارات أزرار الطباعة.
- `npm.cmd run build`: **نجح**، 2509 modules.
- `npm.cmd run test:smoke`: **نجح**؛ المسارات المباشرة أعادت SPA وملف الدخول.
- `npm.cmd run test:reliability`: **نجح**؛ 5/5 ووظائف React ومحاكيات Auth/Firestore/Storage.
- محاولة أولى لتشغيل عدة مجموعات Emulator بالتوازي تعثرت بسبب تنازع المحاكيات (`auth/network-request-failed`). أعيدت الاختبارات اللازمة تسلسليًا ونجحت.
- `npm.cmd run test:db04`: اختبارات الوحدة **5/5 نجحت**، لكن تشغيل Firestore Emulator اللاحق خرج بالرمز 1 دون assertion فاشل. كان الاختبار نفسه ناجحًا بالكامل قبل التعديل الحالي، والتعديل لا يمس Rules أو Indexes.

## ما لم يكن ممكنًا تنفيذه

- لا يمكن إثبات الجهة الموزعة لمواطن لا يحتوي سجله على `assignedGroupId` أو `assignedVolunteerId`. لم يُنشأ ربط تقريبي من عضوية المتطوع أو وجود المجموعة في المشروع.
- لم يتم فحص بصري مصادق عليه ببيانات الإدارة الفعلية أو معاينة الطباعة الأصلية؛ الوصول المحلي توقف عند تسجيل الدخول ولم يتم تجاوز المصادقة.

## القيود المتبقية

- السجلات التاريخية التي لا تحفظ إسنادًا على مستوى المواطن ستظهر بلا جهة مسؤولة.
- إذا كان مطلوبًا لاحقًا إسناد جهة لكل سجل تاريخي، فهذا يحتاج تنظيفًا/ترحيلًا مدروسًا للبيانات بعد مراجعة بشرية، وليس تخمينًا في واجهة التقرير.
- لم تُعدّل Firestore Rules أو قاعدة البيانات، ولم يُنفذ Deploy أو كتابة على Production.

## ملحق: تقرير منفرد، اختيار الأعمدة، ونسخ رابط الصورة

أضيفت التعديلات التالية بناءً على المراجعة اللاحقة:

- لم يعد مسموحًا بإنتاج تقرير تفصيلي يجمع جميع المشاريع. تبقى القائمة والرسم للاستعراض، لكن زر التصدير والتفاصيل الشاملة يتطلبان اختيار مشروع محدد.
- أصبح فلتر المشروع يستخدم `projectId` داخليًا مع عرض اسم المشروع، حتى لا ينتج أكثر من مشروع عند تكرار الأسماء.
- أصبح `בחירת עמודות` يتحكم فعليًا في بطاقات ملخص المشروع وفي أعمدة القسم الأول من التقرير المطبوع. تبقى قوائم المواطنين والمجموعات ضمن التقرير المختار لأنها تفاصيل مرتبطة بالمشروع وليست أعمدة ملخص مشاريع.
- عُدّل عرض الحقول المختارة في الطباعة إلى شبكة تفاصيل RTL من عمودين (اسم الحقل وقيمته) بدل جدول أفقي، بما يطابق نموذج `פרטי הפרויקט` المطلوب.
- أضيف زر `העתקת קישור התמונה` إلى بطاقات الصور العامة في إدارة الصور.
- لا يظهر زر النسخ للصورة الخاصة أو للصورة التي لا تملك `url` عامًا دائمًا. لا تُنسخ روابط المعاينة المؤقتة للصور الخاصة.
- النسخ يستخدم Clipboard API، مع رسالة نجاح أو فشل، ولا يستخدم `execCommand` أو HTML ديناميكيًا.

التحقق بعد الملحق:

- `npm.cmd run test:project-reports`: **نجح، 10/10**، ويتضمن اختبار زر رابط الصور العامة.
- `npm.cmd run build`: **نجح**، 2509 modules.
- لم يتم Deploy أو تعديل بيانات Production.

## ملحق: المتطوع الثابت وتحسين الأداء

- كان عمود `מתנדב קבוע` فارغًا لأن التجميع كان يبحث عن `volunteerName`، بينما النموذج الفعلي للمسن يستخدم `volId` و`volName`.
- أصبح الاسم يؤخذ بالترتيب من لقطة المشارك `volName`، ثم سجل المسن `volName`، ثم اسم وثيقة `volunteers/{volId}` عند توفر المعرّف فقط.
- أضيفت قراءات مجمعة بالمعرّفات `getElderlyByIds` و`getVolunteersByIds` و`getVolunteerGroupsByIds`، باستخدام دفعات `documentId in` وحد أقصى 30 معرّفًا لكل استعلام وتزامن محدود.
- لم يعد تقرير المشاريع يحمل Collections المسنين والمتطوعين والمجموعات كاملة؛ يجلب فقط السجلات المرتبطة بمشاركي المشاريع وإسناداتها.
- أزيل الحساب `participants.filter` المتكرر لكل مجموعة، واستُبدل بخريطة إحصاءات تُبنى في مرور واحد، لتصبح مرحلة التجميع خطية تقريبًا.
- بقي جلب وثائق مشاركي المشاريع ضروريًا لأن الرسم يحتاج حساب `delivery === "נמסר"` الفعلي لكل مشروع، ولا يعتمد على ملخص قديم.

التحقق بعد التحسين:

- `npm.cmd run test:project-reports`: **نجح، 10/10**، ويتضمن التحقق من `volId` و`volName`.
- `npm.cmd run build`: **نجح**، 2509 modules.
