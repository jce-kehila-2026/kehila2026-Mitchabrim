# تقرير إصلاح المرحلة الأولى لنظام إدارة الصور

## 1. النتيجة التنفيذية

تم تنفيذ المرحلة الأولى محليًا دون نشر Hosting أو Rules، ودون تشغيل أي كتابة على
Production.

عولج السبب الجذري للتشوش وخطر النشر العرضي: كان الحقل `isPublic` يؤدي وظيفتين
مختلفتين في الوقت نفسه، إذ كان يحدد قابلية الوصول العام ويؤدي تلقائيًا إلى ظهور
الصورة في الجاليريا، بينما كانت الفئة المحجوزة تغيّر هذا السلوك ضمنيًا. أصبح
الوصول العام والظهور في الجاليريا قرارين مستقلين وواضحين، مع فرض القاعدة:

```text
showInGallery == true  =>  isPublic == true
```

اختيرت إضافة `showInGallery` فقط في هذه المرحلة. لم يُضف `usageType` لأن الكود
الحالي يحفظ استخدام صور أقسام الموقع كروابط URL نصية ولا يملك مراجع موثوقة إلى
وثيقة الصورة؛ إضافة قيمة يدوية أو مستنتجة ستكون مصدر بيانات غير موثوق وقد تعطي
إحساسًا زائفًا بالأمان عند الحذف. تتبع الاستخدام وحماية المراجع مؤجلان بوضوح
للمرحلة الثانية.

## 2. المشكلة قبل الإصلاح

كان نموذج الصورة الأساسي يحتوي على:

- `isPublic`
- `category`
- `url`
- `storagePath`
- بيانات العرض مثل `title` و`notes` و`displayDate`

ولم يكن يحتوي على حقل مستقل لعرض الصورة في الجاليريا. السلوك الفعلي كان:

- صورة عامة من فئة عادية: تظهر تلقائيًا في الجاليريا.
- صورة من الفئة `תמונות אתר פרסומי`: تُجعل عامة تلقائيًا ولا تظهر في الجاليريا.
- تغيير الفئة قد يغيّر الخصوصية أو نتيجة العرض دون عملية مستقلة وصريحة.
- زر أيقونة صغير كان يغيّر الخصوصية فورًا.
- صفحة الإدارة تحمل كامل مجموعة `images` دفعة واحدة.
- قواعد Firestore تحصر الكتابة في Admin، لكنها لم تتحقق من اتساق حالتي
  الخصوصية والجاليريا.
- قاعدة Admin عامة لاحقة في الملف كانت قادرة على تجاوز أي invariant خاص
  بـ`images` إن لم تُستثنَ المجموعة منها.

هذا التصميم جعل معنى الصورة العامة غير واضح، وزاد احتمال نشر صورة في الجاليريا
أو تغيير خصوصيتها بالخطأ.

## 3. ما تم التحقق منه من تقرير التدقيق

تمت قراءة `IMAGE-MANAGEMENT-SAFETY-AUDIT.md` ومقارنة نتائجه بالكود الحالي قبل
التعديل. ثبتت النتائج التالية:

1. `isPublic` كان مستخدمًا للوصول العام ولتحديد عضوية الجاليريا.
2. الفئة الترويجية كانت تؤثر ضمنيًا في الخصوصية والظهور.
3. لم يوجد `showInGallery` أو مرجع استخدام موثوق مثل `usageRefs`.
4. صفحة الإدارة كانت تعتمد على `getAllImages()` وتقرأ المجموعة كاملة.
5. المستخدم العام والمتطوع لا يملكان صلاحية كتابة وثائق الصور، وStorage Rules
   تفرق بين مسارات `public` و`private`.
6. نسخ الرابط كان معروضًا للصورة العامة ذات URL فقط، لكن بقية الإجراءات اعتمدت
   بدرجة كبيرة على أيقونات صغيرة.
7. لا يمكن حاليًا إثبات أن صورة ما مستخدمة في Hero أو قسم عام، لأن أقسام الموقع
   تحفظ URL وليس `imageId`.
8. نقل الملفات بين Storage العام والخاص عملية تعويضية عبر خدمتين، وليست معاملة
   ذرية واحدة.

لم تُنفذ توصية إضافة `usageType` حرفيًا، لأن فحص المصدر أثبت أن المشروع لا يملك
مصدرًا موثوقًا لبنائه أو إبقائه متزامنًا.

## 4. السبب الجذري الذي عولج

السبب الجذري هو دمج ثلاثة مفاهيم مختلفة ضمن `isPublic` و`category`:

1. إمكانية قراءة ملف الصورة ووثيقتها من الجمهور.
2. قرار عرض الصورة في الجاليريا.
3. تصنيف الصورة الإداري أو استخدامها المحتمل في أقسام الموقع.

الفصل بين أول مفهومين يعالج خطر النشر العرضي مباشرة. أما المفهوم الثالث فلا
يمكن حله بصورة سليمة قبل استبدال روابط URL الحرة بمراجع صور أو بناء فهرس استخدام
موثوق، ولذلك لم تُنشأ بيانات تخمينية.

## 5. نموذج البيانات قبل وبعد

### قبل

```js
{
  title,
  category,
  notes,
  url,
  storagePath,
  uploadedAt,
  displayDate,
  isPublic
}
```

### بعد

```js
{
  title,
  category,
  notes,
  url,
  storagePath,
  uploadedAt,
  displayDate,
  isPublic,
  showInGallery
}
```

### معنى الحقول

- `isPublic`: يحدد أن الصورة قابلة للوصول العام. تغيير هذه القيمة يستمر في نقل
  الملف بأمان بين مساري Storage العام والخاص وفق السياسة الحالية.
- `showInGallery`: قرار صريح ومستقل لعرض الصورة في الجاليريا العامة. لا يمكن أن
  يكون `true` إذا كانت الصورة خاصة.
- `category`: تصنيف إداري فقط؛ لم يعد يغيّر `isPublic` أو `showInGallery`.
- `galleryVisibilityLegacy`: قيمة مشتقة داخل الواجهة فقط وليست حقل Firestore.
  تشير إلى أن الوثيقة القديمة لا تحتوي على `showInGallery` بعد.

لم يُضف `usageType`. كما لم يُغيّر نموذج Storage أو روابط أقسام الموقع.

## 6. سلوك الجاليريا قبل وبعد

### قبل

كان معيار العرض:

```js
image.isPublic === true
  && image.category !== PROMOTIONAL_IMAGE_CATEGORY
```

وبذلك كانت معظم الصور العامة تدخل الجاليريا تلقائيًا.

### بعد

- الوثائق الجديدة والمُرحّلة تستخدم `showInGallery` صراحة.
- لا يمكن إضافة صورة خاصة إلى الجاليريا.
- تحويل صورة عامة إلى خاصة يزيلها من الجاليريا في التحديث نفسه.
- جعل صورة خاصة عامة لا يضيفها تلقائيًا إلى الجاليريا.
- تغيير الفئة لا يغيّر الخصوصية أو الظهور.
- الصور القديمة التي لا تحتوي على الحقل تستمر مؤقتًا بالسلوك السابق نفسه، كي لا
  تختفي صور الجاليريا أو الصفحة الرئيسية أثناء فترة النشر والترحيل.

الاستعلام العام بقي محدودًا إلى الصور العامة وفق التدفق الحالي، ثم يطبق
normalizer التوافق مع البيانات القديمة. لم يُطلب index مركب في هذه المرحلة.

## 7. تغييرات واجهة إدارة الصور

تم تحديث صفحة `מאגר תמונות` كما يلي:

- استبدال التحميل الكامل بصفحة أولى من 24 صورة وزر تحميل تدريجي.
- إضافة فلاتر واضحة:
  - الكل.
  - في الجاليريا.
  - عامة وليست في الجاليريا.
  - خاصة.
  - بيانات قديمة تحتاج ترحيلًا.
- توضيح أن البحث والفلاتر يطبقان على الصور المحملة حاليًا.
- عرض شارات نصية واضحة: عامة، خاصة، في الجاليريا، أو legacy.
- فصل الإجراءات إلى أزرار نصية مستقلة:
  - جعل عامة/خاصة.
  - إضافة إلى الجاليريا/إزالة منها.
  - نسخ الرابط العام.
  - حذف.
- نموذج الرفع يطلب قرارين منفصلين للخصوصية والجاليريا، ويعطل خيار الجاليريا
  عندما تكون الصورة خاصة.
- تغيير الفئة في نموذج الرفع أو نافذة التفاصيل لا يغيّر أي حالة نشر.
- الحفاظ على RTL، واستخدام أزرار قابلة للمس على الهاتف بارتفاع مناسب، مع توزيع
  مرن على الهاتف وسطح المكتب.

## 8. التأكيدات والحمايات ضد الخطأ البشري

- تأكيد مستقل قبل جعل الصورة عامة.
- تأكيد مستقل قبل جعلها خاصة، مع توضيح أنها ستُزال من الجاليريا إن كانت ظاهرة.
- تأكيد قبل الإضافة إلى الجاليريا.
- تأكيد قبل الإزالة من الجاليريا.
- الإبقاء على تأكيد الحذف وتحذير عدم إمكانية الاستعادة.
- نسخ الرابط متاح فقط لصورة عامة لها URL دائم؛ يعاد التحقق من الشرط داخل
  handler أيضًا.
- `pendingImageIds` يمنع الضغط المكرر ويعطل إجراءات الصورة كلها أثناء تنفيذ
  عملية حساسة، فلا يمكن بدء نقل وخصوصية وجاليريا متعارضين للصورة نفسها.
- حالات loading مستقلة للتحميل الأول، تحميل المزيد، الرفع، الحذف، والإجراءات.
- رسائل النجاح والفشل مرتبطة بالعملية المحددة بدل أيقونة غامضة.
- قواعد Firestore ترفض حالة `showInGallery: true` مع `isPublic: false` حتى لو
  حاول عميل معدل تجاوز الواجهة.

## 9. التعامل مع الصور القديمة وLegacy

المرحلة الانتقالية متوافقة في الاتجاهين:

- إذا كان `showInGallery` boolean فهو المصدر الصريح للحقيقة.
- إذا كان مفقودًا:
  - الصورة العامة غير الترويجية تبقى ظاهرة كما كانت.
  - الصورة الترويجية أو الخاصة تبقى غير ظاهرة.
- تعرض لوحة الإدارة شارة legacy وفِلترًا لاكتشاف الوثائق غير المُرحّلة.
- قواعد Firestore تقبل قراءة الوثائق القديمة، ولا تتطلب backfill قبل تشغيل
  Hosting الجديد.
- كل رفع جديد يكتب قيمة boolean صريحة.
- العمليات الجديدة تحفظ الاتساق ولا تعتمد على الفئة.

هذه الاستراتيجية تمنع اختفاء الصور أثناء الفصل بين نشر الكود وتشغيل الترحيل.

## 10. Migration / Backfill

أُضيف:

`frontend/scripts/image-gallery-visibility-backfill.mjs`

وظيفته إسناد `showInGallery` للوثائق التي لا تحتوي عليه فقط، وفق السلوك القديم:

- عامة وغير ترويجية: `true`.
- خاصة أو ترويجية: `false`.
- الحقل الصريح الصحيح: يُترك دون تعديل.
- الحقل الصريح من نوع غير صالح: يُعدّ في `invalidExisting` ولا يُصلح تلقائيًا.

خصائص الأمان:

- الوضع الافتراضي `dry-run` ولا يكتب شيئًا.
- حد أقصى 50,000 وثيقة لمنع تشغيل واسع غير مقصود.
- دفعات كتابة من 300.
- precondition باستخدام `updateTime` لمنع الكتابة فوق تغيير أحدث وقع بعد المسح.
- لا يطبع access token أو بيانات اعتماد.
- التشغيل الفعلي خارج Emulator يحتاج جميع الشروط:
  - `--apply`
  - `--production`
  - `--confirm-project=<المشروع المطابق تمامًا>`
  - `GCLOUD_PROJECT`
  - `FIRESTORE_ACCESS_TOKEN`
- تم اختبار dry-run وapply على Firebase Emulator فقط.
- لم يُشغل السكربت ضد Production في هذه المهمة.

أمر dry-run المقترح من `frontend` بعد تجهيز اعتماد قصير العمر محليًا:

```powershell
$env:GCLOUD_PROJECT = "mitchabrim-jce2026"
$env:FIRESTORE_ACCESS_TOKEN = "<SHORT_LIVED_ACCESS_TOKEN>"
npm.cmd run backfill:image-gallery-visibility:dry-run
```

بعد مراجعة الملخص يدويًا، يكون التطبيق الصريح:

```powershell
node scripts/image-gallery-visibility-backfill.mjs --apply --production --confirm-project=mitchabrim-jce2026
```

يجب إزالة متغير token من الجلسة بعد التشغيل وعدم حفظه في ملف أو Git. لا تتضمن
هذه الأوامر أي قيمة اعتماد حقيقية.

## 11. Firestore Rules وIndexes

### Rules

أضيف validator خاص بوثائق `images` يفرض:

- `isPublic`، إن وجد، من نوع boolean.
- `showInGallery`، إن وجد، من نوع boolean.
- `showInGallery == true` يستلزم `isPublic == true`.
- بقاء وثائق legacy المقروءة متوافقة أثناء الترحيل.
- create/update/delete محصورة في Admin فعّال كما كانت.

كذلك استُثنيت مجموعة `images` من fallback الإداري العام اللاحق في الملف، لأن
ذلك fallback كان سيتيح للـAdmin تجاوز validator الخاص بالمجموعة. لم تتوسع
صلاحيات المستخدم العام أو المتطوع.

### Indexes

لم يُعدّل `firestore.indexes.json`. السبب:

- pagination في لوحة الإدارة يستخدم ترتيب document ID وcursor، ولا يحتاج index
  مركبًا.
- الجاليريا تستمر مؤقتًا باستعلام `isPublic` الحالي ثم تطبق توافق legacy في
  العميل.

إضافة استعلام مركب `isPublic + showInGallery` الآن ستخفي وثائق legacy إلى أن
ينتهي backfill، لذلك أُجلت إلى ما بعد اكتمال الترحيل إن أثبت قياس البيانات
الحاجة إليها.

### Storage Rules وFunctions

لم تُعدّل Storage Rules أو Cloud Functions؛ لم يظهر خلل جديد يبرر ذلك في نطاق
المرحلة الأولى.

## 12. أثر الحل على الأمان والأداء

### الأمان

- لا توجد صلاحيات جديدة للمتطوع أو المستخدم العام.
- الكتابة ما زالت Admin فقط.
- invariant موجود في الواجهة والخدمة والقواعد، وليس في الواجهة وحدها.
- جعل الصورة خاصة يزيل حالة الجاليريا.
- لا توجد قيمة سرية أو token في المصدر أو التقرير.

### الأداء والقراءات

- لوحة الإدارة تقرأ 24 وثيقة مبدئيًا بدل كامل المجموعة.
- كل ضغطة تحميل المزيد تقرأ الصفحة التالية فقط.
- الفلاتر والبحث محليان داخل الصفحات التي حُمّلت، ولذلك لا يضيفان قراءات عند
  تبديل الفلتر.
- العمليات تحدّث الصورة المعنية فقط ولا تعيد تحميل المجموعة كاملة.
- الاستعلام العام بقي محدودًا وفق الحدود الحالية ومتوافقًا مع legacy؛ لذلك قد
  يقرأ صورًا عامة غير معروضة ثم يرشحها محليًا. يمكن تقليل ذلك بعد اكتمال
  backfill باستعلام مركب وindex مناسب.
- حجم chunk الرئيسي ما زال يولد تحذير Vite أكبر من 500 kB. البناء ناجح، وهذا
  تحذير موجود على مستوى التطبيق وليس فشلًا خاصًا بإدارة الصور، لكنه يستحق
  code-splitting لاحقًا.

## 13. الملفات المعدلة في هذه المرحلة

- `frontend/src/utils/categorySettings.js`
  - فصل منطق الجاليريا وإضافة normalizer متوافق مع legacy.
- `frontend/src/services/imagesService.js`
  - إضافة pagination، حفظ `showInGallery`، فصل عمليات الخصوصية والجاليريا،
    وفرض الاتساق في طبقة الخدمة.
- `frontend/src/admin/Media.jsx`
  - إعادة تنظيم الإجراءات، الشارات، التأكيدات، loading، منع الضغط المكرر،
    الفلاتر، والتحميل التدريجي.
- `frontend/firestore.rules`
  - فرض أنواع الحقول وشرط عدم ظهور صورة خاصة، ومنع fallback من تجاوز قواعد
    `images`.
- `frontend/scripts/image-gallery-visibility-backfill.mjs`
  - أداة dry-run/backfill آمنة ومتوافقة مع Emulator وProduction المؤكد صراحة.
- `frontend/scripts/run-sec-03-tests.mjs`
  - اكتشاف JDK 21+ المثبت بدل الاعتماد على مسار إصدار واحد ثابت، كي تعمل
    اختبارات القواعد الحالية على بيئة المشروع.
- `frontend/package.json`
  - إضافة أوامر اختبار المرحلة وأمر dry-run.
- `frontend/tests/category-settings.test.mjs`
  - اختبارات السلوك الصريح والتوافق مع legacy.
- `frontend/tests/image-management-phase1.test.mjs`
  - اختبارات الخدمة، الواجهة، الفصل، pagination، وخطة backfill.
- `frontend/tests/sec-03.rules.test.mjs`
  - اختبارات قواعد Firestore/Storage، الصلاحيات، الاستعلامات، pagination،
    وdry-run/apply على Emulator.

لم تُنسب إلى هذه المرحلة تغييرات سابقة موجودة أصلًا في `App.jsx` و`App.css`
و`PublicGallery.jsx` واختباراتها، وتم الحفاظ عليها دون إلغائها.

## 14. الاختبارات المنفذة ونتائجها

### اختبارات المرحلة الأولى

```text
npm.cmd run test:image-management
5 passed, 0 failed
```

غطت:

- خطة backfill وحماية الحقول الصريحة.
- فصل أزرار العامة والجاليريا والنسخ والحذف.
- عدم تغيير الفئة لحالة النشر.
- التحميل التدريجي.
- رفض إضافة صورة خاصة إلى الجاليريا في الخدمة.

### اختبارات التصنيفات والتوافق

```text
npm.cmd run test:categories
6 passed, 0 failed
```

### اختبارات العرض العام والنسخ

```text
node --test tests/media-public-copy.test.mjs tests/public-gallery-presentation.test.mjs
4 passed, 0 failed
```

### اختبارات Firebase Emulator

```text
npm.cmd run test:sec03
passed
```

غطت:

- Admin ينشئ ويعدل ويحذف الصورة.
- anonymous والمتطوع لا يكتبان.
- رفض `showInGallery: true` لصورة خاصة.
- قبول فصل الجاليريا ثم جعل الصورة خاصة.
- قراءة الاستعلام العام.
- pagination الفعلية باستخدام cursor.
- اتساق Firestore وStorage والترحيل الموجود.
- dry-run ثم apply لأداة backfill على Emulator فقط.

ظهور `PERMISSION_DENIED` في مخرجات هذه المجموعة متوقع للحالات السلبية، وانتهى
الأمر برمز خروج 0. ظهر أيضًا تحذير Java من Firebase Storage Rules runtime عن API
deprecated، لكنه لم يؤثر في النتيجة.

إجمالي اختبارات Node المحددة: **15 ناجحة، 0 فاشلة**، بالإضافة إلى مجموعة
Firebase Emulator الناجحة.

## 15. نتيجة Production build

نُفذ:

```text
npm.cmd run build:production
```

النتيجة: **نجح**.

- تم التحقق من متغيرات Production دون طباعة قيمها.
- حوّل Vite عدد 2523 module بنجاح.
- أُنشئ `Media` و`imagesService` ضمن artifact الجديد.
- نجح فحص Hosting artifact الحالي.
- لا يوجد خطأ build.
- التحذير الوحيد هو حجم chunk الرئيسي كما ذُكر أعلاه.

لم يتم نشر مجلد `dist`.

## 16. المخاطر المؤجلة للمرحلة الثانية

1. لا توجد حتى الآن مراجع `imageId` أو `usageRefs` تربط الصور بأقسام Hero
   والمحتوى؛ لذلك لا يستطيع النظام منع حذف أو إخفاء صورة مستخدمة اعتمادًا على
   دليل كامل.
2. الحذف الحالي بين Storage وFirestore ليس معاملة ذرية، وقد يترك حالة جزئية في
   فشل نادر بين الخدمتين.
3. تغيير الخصوصية يستخدم تعويضًا جيدًا لكنه ليس عملية خادمية ذرية.
4. الصور الخارجية لا يملك المشروع دورة حياة ملفها.
5. فلاتر لوحة الإدارة تطبق على الصفحات المحملة فقط. الاستعلامات الخادمية
   المفهرسة يمكن إضافتها لاحقًا إذا كبر الحجم وأصبح البحث الشامل مطلبًا.
6. الاستعلام العام يرشح غير المعروض محليًا أثناء فترة legacy. بعد اكتمال
   backfill يمكن قياس الفائدة من query/index مركب.
7. لا يوجد soft-delete أو سجل تدقيق مركزي لعمليات الصور.

الحل الآمن في المرحلة الثانية هو بناء مراجع استخدام موثوقة أولًا، ثم نقل الحذف
والتغييرات الحساسة إلى Cloud Function تتحقق من المراجع وتنفذ التعويض والتدقيق
مركزيًا.

## 17. ترتيب النشر والترحيل المطلوب لاحقًا

لم يُنفذ أي بند من البنود التالية في هذه المهمة. الترتيب المقترح بعد موافقة
صريحة:

1. أخذ نسخة احتياطية أو export مناسب، والتحقق من المشروع والحساب النشط.
2. نشر Firestore Rules أولًا؛ القواعد الجديدة تقبل legacy وتفرض اتساق الكتابات
   الجديدة:

   ```powershell
   cd frontend
   npx firebase deploy --only firestore:rules --project mitchabrim-jce2026
   ```

3. نشر Hosting من build إنتاجي جديد ومتحقق، وليس artifact قديمًا:

   ```powershell
   npm.cmd run build:production
   npx firebase deploy --only hosting --project mitchabrim-jce2026
   ```

4. تشغيل backfill بوضع dry-run فقط على المشروع المؤكد ومراجعة:
   `scanned` و`planned` و`invalidExisting` وتوزيع legacy.
5. معالجة أي `invalidExisting` يدويًا؛ السكربت لا يغيّرها.
6. تشغيل apply مرة واحدة فقط باستخدام التأكيد الصريح للمشروع.
7. إعادة تشغيل dry-run؛ النتيجة المتوقعة `planned: 0`.
8. اختبار الرفع، جعل عامة/خاصة، الإضافة/الإزالة من الجاليريا، النسخ والحذف على
   حساب Admin، ثم التحقق من الجاليريا العامة.
9. مراقبة Logs وأخطاء Rules/Storage بعد النشر.

### الموارد التي تحتاج نشرًا أو إجراءً

- **Hosting:** نعم، لنشر الواجهة والخدمة الجديدة.
- **Firestore Rules:** نعم، لفرض invariant في الخادم.
- **Firestore data backfill:** نعم، إجراء يدوي بعد dry-run والمراجعة.
- **Firestore Indexes:** لا في هذه المرحلة.
- **Storage Rules:** لا.
- **Cloud Functions:** لا.

## 18. الحكم النهائي

إصلاح المرحلة الأولى **مكتمل ومتحقق محليًا**. يحتاج لاحقًا إلى نشر Hosting
وFirestore Rules بالترتيب المذكور، ثم dry-run ومراجعة وتشغيل backfill بموافقة
منفصلة. لم يتم أي Deploy أو تعديل Production.
