# BACKUP SETTINGS AUDIT

## האם קיים Backup אמיתי?

לא. בקוד המקומי אין Cloud Function, ייצוא Firestore, גיבוי Storage, משימה מתוזמנת, שירות גיבוי או כתיבה לאוסף `backups`/`backupLogs`. גם ברשימת ה-Functions וה-Extensions בפרויקט `mitchabrim-jce2026` לא נמצא משאב גיבוי.

## מקור תאריך הגיבוי האחרון

הטקסט `28.05.2026 • הצליח` היה ערך קבוע בתוך `frontend/src/admin/Settings.jsx`, ללא מקור נתונים אמיתי.

## מדוע הכפתור לא עבד?

לכפתור `הפעל גיבוי` לא היה `onClick`, שירות או endpoint בצד השרת. הוא היה רכיב תצוגה בלבד.

## מה שונה?

- הוסר תאריך הגיבוי הפיקטיבי.
- נוסף הסבר ברור שגיבוי הנתונים אינו מופעל.
- הכפתור הושבת ושמו שונה ל-`הגיבוי אינו מופעל`.

## האם הכפתור נשאר?

הכפתור נשאר במצב מושבת כדי להבהיר למנהל שהיכולת אינה זמינה, בלי להעמיד פנים שמתבצעת פעולה.

## מה דורש Deploy בהמשך?

כדי לפרסם את התיקון הנוכחי נדרש Hosting deploy בלבד. לא נדרש Functions, Rules או Indexes deploy.

## עלות או הגדרה חיצונית

הפעלת גיבוי אמיתי בעתיד דורשת בחירה והגדרה מפורשת בצד Google Cloud, כגון Firestore managed backups/PITR או export מתוזמן ל-Cloud Storage, כולל IAM, מדיניות שמירה ועלויות אחסון/תפעול בהתאם לפתרון. עדיף גיבוי מתוזמן ומנוטר מחוץ לממשק על פני כפתור ידני בלבד.
