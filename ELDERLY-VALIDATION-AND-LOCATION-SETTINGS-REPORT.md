# ELDERLY VALIDATION AND LOCATION SETTINGS REPORT

## الوضع السابق

- نموذج الإضافة والتعديل هو النموذج نفسه، لكنه لم يفرض أطوال رقم الهوية والهاتف بصورة متطابقة، وكان الهاتف الأرضي يسمح بعشرة أرقام.
- تاريخ الميلاد استخدم حقل تاريخ، من دون تحقق مشترك يمنع التاريخ الناقص أو المستحيل أو المستقبلي قبل الحفظ.
- الدولة كانت قائمة ثابتة داخل الواجهة، ولغة التحدث قيمة مفردة.
- إعدادات المناطق والأحياء دعمت الإضافة والحذف، ولم تدعم إعادة التسمية أو نقل حي مع تحديث المراجع المنسوخة.

## التعديلات المنفذة

- أضيف تحقق مشترك في نموذج الإضافة والتعديل:
  - رقم الهوية: اختياري، وعند إدخاله يجب أن يكون 9 أرقام.
  - الهاتف المحمول: مطلوب و10 أرقام.
  - الهاتف الأرضي: اختياري و9 أرقام.
  - الإدخال رقمي فقط، والقيم تبقى `String` لحفظ الصفر الأول.
  - رسائل الخطأ في الواجهة بالعبرية، والحفظ يتوقف عند الخطأ.
- أضيف تحقق صارم لتاريخ الميلاد بصيغة التخزين الحالية `YYYY-MM-DD`، مع رفض التاريخ الناقص والمستحيل والمستقبلي.
- أضيف التحقق نفسه على الخادم داخل `mutateElderly` لمنع تجاوز تحقق الواجهة.
- أصبحت الدول محملة من `settings/general.countries` مع دمج القائمة الحالية كقيمة ابتدائية، وترتيب عبري، وإضافة دولة جديدة من النموذج.
- منع تكرار الدولة بعد Unicode normalization، وإزالة الفراغات الزائدة، والمقارنة غير الحساسة لحالة الأحرف. تضاف الدولة بمعاملة Firestore وتظهر في الاختيارات اللاحقة.
- أصبح اختيار اللغات متعددًا. تحفظ اللغات الجديدة في `languages: string[]`، ويستمر حفظ `language` كنص مجمع للتوافق مع البحث والتقارير والطباعة الحالية.
- البيانات القديمة التي تحتوي `language` واحدة تتحول للعرض والتعديل تلقائيًا إلى قائمة من عنصر واحد.
- أضيف حقل لإضافة لغة غير موجودة. تحفظ اللغات المخصصة في `settings/general.languages` بمعاملة Firestore، مع تطبيع الاسم ومنع التكرار، وتظهر لاحقًا في النموذج وتُحدد مباشرة بعد إضافتها.
- أضيف تعديل اسم المنطقة، تعديل اسم الحي، ونقل حي إلى منطقة أخرى، مع منع الأسماء الفارغة والمكررة وعرض تأكيد قبل تنفيذ تحديث متعدد السجلات.
- أعيد تصميم قسم المناطق والأحياء بنفس لغة بطاقات الفئات: بطاقة مستقلة لكل منطقة، عنوان ثابت، وأحياء على شكل وسوم. استبدل زر الحذف الظاهر بقائمة ثلاث نقاط؛ قائمة الحي تعرض `עריכה` و`העברה` و`מחיקה`، وقائمة المنطقة تعرض التعديل والحذف.
- لا تظهر حقول التعديل أو اختيار منطقة النقل إلا بعد اختيار الإجراء، لتقليل ازدحام الصفحة.
- صُغّر زر النقاط الثلاث وأيقونته في رأس المنطقة ووسم الحي.
- لم يحذف أي حي أو منطقة أو بيانات ضمن هذه المهمة.

## تحديث الأحياء والمناطق والمراجع

التعديل متعدد السجلات يمر عبر Cloud Function إدارية جديدة باسم `updateLocationSettings`، مع التحقق من أن المستخدم Admin نشط وفرض App Check. تستخدم الدالة BulkWriter وتحدّث فقط الحقول المعروفة في النموذج الحالي:

- الجذور: `elderly` و`volunteers` و`parliaments`.
- النسخ المرتبطة: collection groups باسم `elderlyParticipants` و`participants`.
- بيانات الاتصال المنسوخة: `elderlyContacts.elderlyArea` و`elderlyContacts.elderlyNeighborhood`.
- إعدادات `settings/general.areas`.

عند تعديل حي، يقيّد التحديث بالمنطقة الأصلية متى كان حقل المنطقة موجودًا في السجل، لتجنب تحديث سجل يحمل اسم حي مشابه في منطقة أخرى. لا يوجد Migration ولا كتابة على Production في هذا التنفيذ.

في تشغيل Vite المحلي لا يوجد `VITE_FIREBASE_APPCHECK_SITE_KEY`، لذلك كان `getSecureFunctions()` يعيد `null` وتفشل إعادة التسمية والنقل قبل بدء العملية. أضيف مسار تطوير محلي فقط (`import.meta.env.DEV`) ينفذ التحديث نفسه مباشرة عبر Firestore بصلاحيات Admin الموجودة في Rules وعلى دفعات لا تتجاوز 400 كتابة. Build الإنتاجي لا يسمح بهذا المسار ويظل معتمدًا على Cloud Function المحمية بـApp Check.

بعد التجربة الفعلية ظهر أن الاستعلامات المفلترة على collection group باسم `participants` تحتاج فهارس `COLLECTION_GROUP_ASC` غير موجودة للحقول `area` و`neighborhood`. أزيل الاعتماد على هذه الفهارس من مساري التطوير وCloud Function: تقرأ مجموعات المشاركين دون فلتر مركب ثم تطبق مطابقة دقيقة داخل الكود قبل إدراج أي كتابة. لذلك لا يلزم إنشاء أو نشر فهارس جديدة لهذا الإصلاح.

## الملفات المعدلة

- `frontend/src/admin/Elderly.jsx`
- `frontend/src/admin/Settings.jsx`
- `frontend/src/services/settingsService.js`
- `frontend/src/utils/elderlyFormModel.js`
- `frontend/functions/index.js`
- `frontend/functions/src/elderlyMutationCore.js`
- `frontend/functions/src/locationSettingsCore.js`
- `frontend/tests/elderly-validation-location.test.mjs`
- `frontend/package.json`

## الاختبارات والنتائج

- `npm.cmd run test:elderly-settings`: نجح، 7/7، ويشمل تحققًا ساكنًا لمسار حفظ اللغة المخصصة وقائمة الإجراءات الجديدة.
  - أطوال 8/9/10/11، والحفاظ على الصفر الأول.
  - تاريخ صحيح/ناقص/مستحيل/مستقبلي.
  - تحقق الخادم المطابق.
  - لغة قديمة مفردة ولغات متعددة دون تكرار.
  - إعادة تسمية منطقة وحي، ونقل حي، ومنع التكرار.
- `npm.cmd run build`: نجح، 2510 module.
- `node --check` لملفات Functions المعدلة: نجح.
- `git diff --check`: نجح.
- اختبار قاعدة البيانات الحالي `test:db01` شُغّل أثناء التحقق: اختبارات الوحدة نجحت 6/6، لكن تشغيل Firestore Emulator فشل في البيئة برمز 1 قبل اختبارات التكامل؛ لا توجد نتيجة تكامل Emulator يمكن الادعاء بها.

## الفحص اليدوي والقيود

- تعذر فتح النماذج المحمية تفاعليًا من أداة المتصفح المحلية: أداة المعاينة لم تستطع الوصول إلى مسار المشروع، كما أن تدفقات الإدارة تحتاج جلسة Admin. لم يتم تجاوز المصادقة ولم تستخدم بيانات Production.
- إضافة الدولة تكتب إلى مستند الإعدادات الحالي بصلاحيات Admin؛ لم يلزم تعديل Firestore Rules.
- دالة تحديث المراجع جديدة محليًا فقط. لم يتم Deploy حسب الطلب، ولذلك لن تعمل إعادة تسمية/نقل المناطق على النسخة المنشورة قبل نشر Functions بموافقة منفصلة.
- تحديث عدة Collections ليس معاملة Firestore ذرية واحدة؛ تنفذ الدالة الكتابات أولًا ثم تحفظ الإعدادات. BulkWriter مناسب للأحجام الكبيرة، لكن الانقطاع النادر أثناء العملية قد يحتاج إعادة تنفيذ العملية نفسها بعد فحص السجلات.
- لم تُغيّر طريقة تخزين تاريخ الميلاد، ولم يُحذف أي سجل أو إعداد.

## الحكم النهائي

التغييرات مطبقة محليًا وBuild والاختبارات المخصصة ناجحة. تحتاج وظيفة تعديل المناطق والأحياء إلى نشر لاحق منفصل حتى تصبح متاحة في Production، ويظل الفحص التفاعلي بجلسة Admin واختبار Emulator الكامل مطلوبين قبل اعتبارها متحققة بالكامل على البيئة المنشورة.
