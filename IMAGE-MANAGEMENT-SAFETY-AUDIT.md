# تقرير تدقيق سلامة نظام إدارة الصور

## نطاق التدقيق

تم فحص نظام إدارة الصور في صفحة `מאגר תמונות` دون تنفيذ أي تعديل على كود التطبيق أو قواعد Firebase أو البيانات.

شمل الفحص:

- واجهة إدارة الصور والإجراءات المتاحة على بطاقات الصور.
- إنشاء الصور وتعديلها وتغيير خصوصيتها وحذفها.
- استعلامات الجاليريا في الصفحة الرئيسية وصفحة الجاليريا العامة.
- حقول الصور داخل محتوى الموقع العام.
- دورة حياة الملفات بين Firestore وFirebase Storage.
- صلاحيات Firestore وStorage.
- الاختبارات وأداة ترحيل الصور القديمة الموجودة في المشروع.

## الخلاصة التنفيذية

المشكلة الأساسية ليست ضعف صلاحيات Firebase، بل أن نموذج البيانات الحالي يستخدم الحقل `isPublic` للتعبير عن أكثر من معنى:

1. هل ملف الصورة متاح للقراءة العامة؟
2. هل يجب عرض الصورة داخل الجاليريا العامة؟
3. هل الصورة مخصصة لاستخدامها داخل أحد أقسام الموقع؟

لا توجد حقول مستقلة مثل `showInGallery` أو `usageType` أو مراجع تربط الصورة بالأقسام التي تستخدمها. نتيجة ذلك يمكن نشر صورة في الجاليريا بمجرد جعلها عامة، ويمكن حذف صورة مستخدمة في الصفحة الرئيسية لأن النظام لا يعرف أنها مستخدمة أصلًا.

الواجهة تزيد احتمال الخطأ البشري لأن تغيير الخصوصية يتم فورًا من أيقونة صغيرة دون تأكيد. أما الحذف فله تأكيد، لكنه تأكيد عام لا يعرض مواضع استخدام الصورة ولا يمنع حذف صورة مرتبطة.

قواعد Firestore وStorage تمنع المتطوع والمستخدم غير المخول من تعديل الصور، وهي جيدة من ناحية فصل صلاحيات Admin. لكنها لا تفرض اتساقًا بنيويًا بين `isPublic` ومسار الملف في Storage، ولا تستطيع حماية مراجع الصور المخزنة كرابط نصي داخل وثائق أخرى.

## نموذج بيانات الصور الحالي

عند رفع صورة جديدة، تُنشأ وثيقة في مجموعة `images` بالحقول التالية:

- `title`
- `category`
- `notes`
- `url`
- `storagePath`
- `uploadedAt`
- `displayDate`
- `isPublic`

الدليل: `frontend/src/services/imagesService.js`، الأسطر 101-130.

لا توجد حاليًا حقول مثل:

- `showInGallery`
- `usageType`
- `usageRefs`
- `linkedSections`
- `deletedAt`
- `status`

الحقل `category` هو تصنيف إداري، لكنه يُستخدم أيضًا بطريقة غير مباشرة لتحديد سلوك النشر. الفئة المحجوزة:

`תמונות אתר פרסומי`

تجبر الصورة على البقاء عامة، ويتم استبعادها من الجاليريا.

الدليل:

- `frontend/src/utils/categorySettings.js`، الأسطر 1-12.
- `frontend/src/utils/categorySettings.js`، الأسطر 72-78.
- `frontend/src/services/imagesService.js`، الأسطر 109-126.

## الفرق الفعلي بين أنواع الصور

### صورة عامة

الصورة تعتبر عامة عندما:

- `isPublic === true`
- الملف موجود غالبًا تحت `images/public/...`
- وثيقة Firestore قابلة للقراءة العامة.
- رابط التنزيل محفوظ في `url`.

قواعد Storage تسمح للجميع بقراءة الملفات الموجودة تحت `images/public`.

الدليل:

- `frontend/storage.rules`، الأسطر 60-69.
- `frontend/firestore.rules`، الأسطر 555-574.
- `frontend/src/services/imageStoragePolicy.js`، الأسطر 1-5 و25-28.

### صورة تظهر في الجاليريا

الصورة تظهر في الجاليريا عندما:

```js
image.isPublic === true
  && image.category !== PROMOTIONAL_IMAGE_CATEGORY
```

أي أن `isPublic` لا يعني قابلية القراءة فقط، بل يؤدي أيضًا إلى عرض الصورة تلقائيًا في الجاليريا إذا لم تكن ضمن الفئة الترويجية.

الدليل:

- `frontend/src/utils/categorySettings.js`، الأسطر 72-74.
- `frontend/src/services/imagesService.js`، الأسطر 74-94.
- `frontend/src/components/public/GallerySection.jsx`، تحميل الصور العامة ثم عرض أول خمس صور.
- `frontend/src/pages/PublicGallery.jsx`، تحميل حتى 200 صورة عامة مؤهلة للجاليريا.

### صورة مستخدمة في أقسام الموقع

لا يوجد تمثيل رسمي لهذا النوع داخل وثيقة الصورة.

محتوى الموقع يحتوي حقول روابط صور مستقلة، مثل:

- `hero.imageMain`
- `hero.imageTopLeft`
- `hero.imageBottom`
- `about.image`
- `quote.image`
- `activities.details.*.image`
- `press.facebook.image`
- `press.ynet.image`
- صور الشركاء وأعضاء الفريق

الدليل:

- `frontend/src/services/siteContentService.js`، الأسطر 6-95.
- `frontend/src/admin/SiteContent.jsx`، الأسطر 172-202 و229-258 و285 وما بعدها.
- `frontend/src/components/admin/site-content/fields.jsx`، الأسطر 34-77.

حقل الصورة في إدارة محتوى الموقع هو حقل URL نصي. لا يحفظ `imageId` ولا `storagePath` ولا ينشئ مرجعًا عكسيًا داخل وثيقة الصورة.

لذلك قد تكون صورة من `מאגר תמונות` مستخدمة في Hero أو About أو Press، لكن مجموعة `images` لا تعرف ذلك.

### صورة خاصة أو داخلية

الصورة الخاصة يفترض أن تكون:

- `isPublic === false`
- موجودة تحت `images/private/...`
- لا تحتوي رابط تنزيل عام في `url`.
- قابلة للقراءة فقط بواسطة Admin فعّال.

واجهة Admin تقرأ الصورة الخاصة باستخدام `getBlob` ثم تنشئ رابط معاينة مؤقتًا داخل المتصفح.

الدليل:

- `frontend/src/services/imagesService.js`، الأسطر 57-70.
- `frontend/src/services/imageStoragePolicy.js`، الأسطر 53-59.
- `frontend/storage.rules`، الأسطر 71-80.

## هل يكفي تغيير `isPublic` لنشر صورة في الجاليريا؟

نعم، في معظم الحالات.

عند الضغط على أيقونة الكرة الأرضية/القفل في البطاقة:

1. يتم قلب قيمة `isPublic`.
2. ينقل الملف بين مسار `public` ومسار `private`.
3. تحدث وثيقة Firestore.
4. إذا أصبحت الصورة عامة ولم تكن فئتها `תמונות אתר פרסומי`، تدخل تلقائيًا في استعلام الجاليريا.

لا يوجد حقل مستقل للموافقة على الظهور في الجاليريا.

الدليل:

- `frontend/src/admin/Media.jsx`، الأسطر 202-220.
- `frontend/src/admin/Media.jsx`، الأسطر 648-700.
- `frontend/src/services/imagesService.js`، الأسطر 136-189 و204-213.
- `frontend/src/utils/categorySettings.js`، الأسطر 72-78.

### حالة خطرة مرتبطة بتغيير الفئة

عند اختيار الفئة الترويجية، تضبط الواجهة `isPublic` إلى `true` تلقائيًا.

عند تغيير الفئة لاحقًا إلى فئة عادية، لا تعيد الواجهة `isPublic` إلى `false`. لذلك قد تتحول الصورة، بعد الحفظ، إلى صورة ظاهرة في الجاليريا دون أن يقصد المسؤول ذلك.

الدليل:

- `frontend/src/admin/Media.jsx`، الأسطر 1026-1031.
- `frontend/src/admin/Media.jsx`، الأسطر 1347-1357.

## سلامة حذف الصور

### التأكيد الموجود حاليًا

الحذف لا يحدث مباشرة من أول ضغطة. تظهر نافذة تأكيد تعرض اسم الصورة وتحذر من عدم إمكانية استعادتها.

الدليل:

- `frontend/src/admin/Media.jsx`، الأسطر 175-200.
- `frontend/src/admin/Media.jsx`، الأسطر 1479-1576.

هذا أفضل من الحذف الفوري، لكنه غير كافٍ لحماية الصور المرتبطة.

### عدم وجود فحص للمراجع

قبل الحذف لا يفحص النظام:

- وثيقة `siteContent/home`.
- صور Hero أو About أو Quote أو Press.
- صفحات تفاصيل الأنشطة.
- الشركاء وأعضاء الفريق.
- أي وثيقة أخرى قد تحتوي رابط الصورة.

لا يوجد `usageRefs` أو استعلام يحدد أين تستخدم الصورة.

لذلك يمكن حذف صورة مستخدمة فعليًا في الموقع العام، ويبقى رابطها محفوظًا داخل محتوى الموقع بعد حذف الملف.

النتيجة المتوقعة:

- صورة مكسورة في الموقع العام.
- Placeholder أو مساحة فارغة حسب المكون.
- عدم وجود وسيلة تلقائية لمعرفة سبب الانكسار.

### ترتيب الحذف الحالي

الدالة الحالية:

1. تحذف ملف Storage.
2. تحذف وثيقة Firestore.

الدليل: `frontend/src/services/imagesService.js`، الأسطر 215-223.

إذا نجح حذف الملف وفشل حذف الوثيقة، تبقى وثيقة Firestore تشير إلى ملف غير موجود. هذا يخلق حالة غير متناسقة.

أما الصور الخارجية التي لا يملك النظام مسار Storage لها، فيحذف النظام الوثيقة فقط، بينما يبقى الملف الخارجي خارج سيطرته.

## سلامة تغيير الخصوصية

### النقل بين public وprivate

الخدمة تحاول تنفيذ النقل بصورة تعويضية:

1. قراءة الملف الأصلي.
2. رفع نسخة إلى المسار الجديد.
3. تحديث Firestore.
4. حذف الملف القديم.
5. عند الفشل، محاولة إعادة Firestore إلى الحالة السابقة وحذف النسخة الجديدة.

الدليل: `frontend/src/services/imagesService.js`، الأسطر 136-189.

هذا أفضل من تحديث `isPublic` فقط، لكنه ليس معاملة ذرية؛ Firestore وStorage خدمتان منفصلتان.

توجد حالة فشل طرفية:

- ينجح تحديث Firestore إلى المسار الجديد.
- يفشل حذف الملف القديم.
- تفشل محاولة إعادة Firestore.
- تُحذف النسخة الجديدة ضمن cleanup.

في هذه الحالة قد تشير الوثيقة إلى ملف جديد تم حذفه، بينما يبقى الملف القديم موجودًا.

### اتساق المسار مع `isPublic`

التطبيق يفترض:

- `isPublic=true` يقابله `images/public/...`
- `isPublic=false` يقابله `images/private/...`

لكن Firestore Rules تسمح للـAdmin بكتابة أي شكل لوثيقة الصورة، ولا تتحقق من هذا الاتساق.

لذلك القواعد لا تمنع، مثلًا:

- وثيقة خاصة تشير إلى ملف تحت `images/public`.
- وثيقة عامة تشير إلى ملف تحت `images/private`.
- صورة خاصة تحتفظ برابط تنزيل عام قديم.

توجد دالة `planImageMigration` لاكتشاف هذه الحالات، لكن أداة الترحيل الموجودة مصممة للعمل على Emulator فقط وترفض Production صراحة.

الدليل:

- `frontend/src/services/imageStoragePolicy.js`، الأسطر 61-80.
- `frontend/scripts/sec-03-migrate-images.mjs`، الأسطر 101-108.

لذلك يمكن إثبات وجود احتمال عدم اتساق في التصميم، لكن لا يمكن إثبات عدد الحالات الموجودة فعليًا في بيانات Production دون تنفيذ فحص قراءة مخصص وآمن.

## تقييم وضوح الواجهة

### الأزرار الحالية

كل بطاقة تحتوي أزرارًا دائرية صغيرة:

- سلة مهملات للحذف.
- كرة أرضية للصورة العامة.
- قفل للصورة الخاصة.
- زر نسخ الرابط للصورة العامة.

الدليل: `frontend/src/admin/Media.jsx`، الأسطر 602-728.

### المشكلات البصرية ومخاطر الخطأ البشري

- حجم أزرار الحذف والخصوصية 30×30 بكسل فقط.
- زر الحذف وزر تغيير الخصوصية موجودان فوق الصورة مباشرة.
- حالة الخصوصية تعتمد على الأيقونة واللون.
- `title` يعطي شرحًا عند تمرير الماوس، لكنه لا يساعد مستخدم الهاتف.
- زر الحذف وزر الخصوصية لا يحتويان `aria-label` واضحًا، بينما زر نسخ الرابط يحتوي `aria-label`.
- لا توجد شارة نصية ثابتة مثل: `פרטית` أو `בגלריה` أو `בשימוש באתר`.
- لا توجد قائمة إجراءات تفصل الإجراءات اليومية عن الإجراء المدمر.
- لا توجد حالة pending تمنع تكرار الضغط أثناء نقل الملف.

### التأكيدات الحالية

| العملية | تأكيد حالي | التقييم |
|---|---:|---|
| رفع صورة خاصة | لا | مقبول |
| رفع صورة عامة | لا | مخاطرة متوسطة |
| جعل صورة عامة من البطاقة | لا | مخاطرة مرتفعة لأنها قد تدخل الجاليريا |
| تحويل صورة إلى خاصة | لا | مخاطرة مرتفعة لأنها قد تكسر موضع استخدامها |
| تعديل الخصوصية من نافذة التفاصيل | لا يوجد تأكيد مستقل؛ تحفظ مع بقية الحقول | مخاطرة متوسطة |
| حذف صورة | نعم | غير كافٍ لأنه لا يفحص الاستخدام |

## الصلاحيات

### Firestore

مجموعة `images` تسمح:

- للـAdmin الفعّال: قراءة وكتابة وحذف.
- للعامة: قراءة الوثائق التي `isPublic == true`.
- للمتطوع: لا توجد صلاحية كتابة.

تعريف Admin يتطلب:

- تسجيل الدخول.
- وجود وثيقة مستخدم.
- `status == "active"`.
- `role == "admin"`.

الدليل:

- `frontend/firestore.rules`، الأسطر 10-35.
- `frontend/firestore.rules`، الأسطر 555-574.

### Storage

- الملفات العامة قابلة للقراءة من الجميع.
- الملفات الخاصة وملفات المسار القديم قابلة للقراءة بواسطة Admin فقط.
- الكتابة في مسارات الصور محصورة في Admin فعّال.
- الرفع مقيد بصورة وحجم أقصى 5MB.

الدليل: `frontend/storage.rules`، الأسطر 10-51 و59-80.

### الحكم الأمني

لا يوجد دليل في القواعد الحالية على أن متطوعًا أو مستخدمًا غير مخول يستطيع تغيير خصوصية صورة أو حذفها.

المشكلة الأساسية هي سلامة عمليات Admin ومنع الخطأ البشري، وليست توسع صلاحيات المتطوع.

يوجد نقص اختباري: اختبارات SEC-03 تتحقق من قراءة Storage ومنع كتابة anonymous، لكنها لا تحتوي حالات صريحة كافية تثبت منع المتطوع من إنشاء/تعديل/حذف وثائق `images` في Firestore.

## نسخ روابط الصور

الواجهة تعرض زر نسخ الرابط فقط عندما:

```js
img.isPublic && img.url
```

كما تعيد الدالة التحقق من الشرط نفسه قبل النسخ.

الدليل: `frontend/src/admin/Media.jsx`، الأسطر 222-235 و702-728.

هذا السلوك صحيح في الواجهة.

لكن وجود `isPublic=false` في Firestore لا يجعل ملفًا تحت `images/public` خاصًا؛ صلاحية القراءة في Storage تعتمد على المسار. لذلك تبقى سلامة نسخ الرابط مرتبطة باتساق `isPublic` مع `storagePath`.

## سبب تشوش الواجهة

الأسباب الرئيسية:

1. لا يوجد فصل بصري أو مفاهيمي بين الخصوصية والظهور في الجاليريا والاستخدام داخل الموقع.
2. الأيقونة نفسها تنفذ إجراءً كبير الأثر فورًا.
3. لا توجد فلاتر حسب حالة الاستخدام، مثل:
   - صور الجاليريا.
   - صور أقسام الموقع.
   - صور عامة غير معروضة.
   - صور خاصة.
4. الصفحة تستخدم `getAllImages()` وتحمل جميع الوثائق دفعة واحدة دون pagination.
5. لا توجد شارات نصية أو ملخص حالة داخل البطاقة.
6. الإجراء المدمر موجود بجانب إجراء تغيير الخصوصية.
7. الفئة الترويجية تُستخدم لتعويض غياب نموذج استخدام واضح.

## الحل الأول: تحسين الواجهة الحالية

### الفكرة

الإبقاء على `isPublic` ونموذج البيانات الحالي، مع جعل الإجراءات أوضح وأكثر أمانًا.

### التعديلات المقترحة

- استبدال الأيقونات المنفردة بقائمة `פעולות`.
- عرض شارات نصية واضحة:
  - `פרטית`
  - `ציבורית`
  - `מוצגת בגלריה`
  - `תמונת אתר פרסומי`
- إضافة تأكيد قبل:
  - النشر.
  - التحويل إلى خاص.
  - تغيير فئة قد يؤدي إلى ظهور الصورة في الجاليريا.
- توضيح الأثر داخل التأكيد.
- تعطيل الأزرار أثناء العملية.
- إضافة فلاتر حسب الخصوصية والفئة.
- إضافة pagination أو تحميل تدريجي.
- تحسين `aria-label` ومساحات اللمس على الهاتف.
- جعل الحذف داخل قائمة منفصلة بلون تحذيري.

### المزايا

- أسرع تنفيذًا.
- لا يحتاج ترحيل بيانات.
- لا يحتاج تغيير الاستعلامات العامة.
- يقلل الأخطاء الناتجة عن الأيقونات.

### العيوب

- يبقى `isPublic` مرتبطًا تلقائيًا بالجاليريا.
- لا يعرف النظام أين تستخدم الصورة.
- لا يمنع حذف صورة مرتبطة.
- لا يحل الاتساق بين Firestore وStorage بصورة كاملة.

### الموارد المطلوبة

- `frontend/src/admin/Media.jsx`
- اختبارات واجهة/اختبارات ثابتة جديدة.

لا يلزم تعديل Firestore Rules أو Storage Rules لهذا الحل.

## الحل الثاني: فصل الخصوصية عن الجاليريا والاستخدام

### نموذج مقترح

```js
{
  isPublic: true,
  showInGallery: false,
  usageType: "site-section",
  category: "תמונות אתר פרסומי"
}
```

قيم `usageType` الممكنة:

- `gallery`
- `site-section`
- `shared`
- `internal`

ويجب أن يكون هناك invariant:

```text
showInGallery == true  =>  isPublic == true
```

### السلوك المقترح

- `isPublic`: هل الملف قابل للوصول العام؟
- `showInGallery`: هل يظهر في الجاليريا؟
- `usageType`: وصف إداري للغرض.
- الفئة تبقى للتصنيف فقط، ولا تغير الخصوصية تلقائيًا.
- نشر الصورة لا يضيفها تلقائيًا إلى الجاليريا.
- إضافة الصورة إلى الجاليريا تكون عملية مستقلة وواضحة.

### ترحيل البيانات

قيمة `showInGallery` الأولية يمكن اشتقاقها:

```js
showInGallery =
  isPublic === true
  && category !== PROMOTIONAL_IMAGE_CATEGORY
```

أما `usageType` فلا ينبغي اشتقاقه من الفئة وحدها. يجب فحص روابط الصور داخل `siteContent/home` والقوائم الأخرى لتحديد الاستخدام الفعلي.

### المزايا

- يزيل الخلط الأساسي.
- يمنع نشر صورة في الجاليريا بمجرد جعلها عامة.
- يسمح بفلاتر وحالات واضحة.
- يجعل سلوك الفئة الترويجية مفهومًا بدل الاعتماد على استثناء مخفي.

### العيوب

- يحتاج ترحيل البيانات الحالية.
- يحتاج تعديل استعلامات الجاليريا.
- يحتاج Firestore index للاستعلام المشترك.
- `usageType` وحده لا يمنع الحذف ولا يثبت الاستخدام الفعلي.

### الموارد المطلوبة

- `frontend/src/admin/Media.jsx`
- `frontend/src/services/imagesService.js`
- `frontend/src/utils/categorySettings.js`
- `frontend/src/components/public/GallerySection.jsx`
- `frontend/src/pages/PublicGallery.jsx`
- `frontend/firestore.rules`
- `frontend/firestore.indexes.json`
- اختبارات القواعد والجاليريا.
- سكربت backfill آمن.

لا يلزم تغيير Storage Rules لمجرد إضافة هذه الحقول.

## الحل الثالث: حماية مرجعية ومنع الحذف أو الإخفاء الخطر

### الفكرة

تطبيق الفصل الموجود في الحل الثاني، وإضافة مراجع حقيقية للصورة.

نموذج محتمل:

```js
{
  isPublic: true,
  showInGallery: false,
  usageType: "site-section",
  usageRefs: [
    {
      resource: "siteContent/home",
      field: "hero.imageMain"
    }
  ]
}
```

الأفضل على المدى الطويل أن تحفظ أقسام الموقع `imageId` بدل URL فقط، مع توليد الرابط من وثيقة الصورة.

### سياسة الحذف المقترحة

- إذا كانت `usageRefs` غير فارغة:
  - يمنع الحذف.
  - يمنع التحويل إلى خاص إذا كان القسم عامًا.
  - تعرض للمسؤول قائمة مواضع الاستخدام.
  - يطلب استبدال الصورة أو فك الارتباط أولًا.
- إذا لم تكن الصورة مستخدمة:
  - تبدأ عملية حذف آمنة.
  - توسم الوثيقة `status: "deleting"` أو `deletedAt`.
  - يحذف الملف.
  - تحذف/تؤرشف الوثيقة بعد نجاح العملية.
- عند فشل Storage لا تفقد الوثيقة المرجعية.

### مكان تنفيذ العمليات

يفضل تنفيذ تغيير الخصوصية والحذف الحساس بواسطة Callable Cloud Function:

- تتحقق من Authentication.
- تتحقق من App Check.
- تتحقق من دور Admin.
- تفحص المراجع.
- تمنع التعارض.
- تسجل العملية.
- تنفذ التعويض الآمن عند الفشل.

يمكن عندها منع عمليات الحذف وتغيير الحقول الحساسة مباشرة من العميل عبر Firestore Rules.

### المزايا

- أعلى حماية من الحذف العرضي.
- يمنع كسر Hero وأقسام الموقع.
- يوفر أثر استخدام واضحًا.
- يحسن التعامل مع الفشل الجزئي.
- يسمح برسائل دقيقة للمسؤول.

### العيوب

- أكبر من الحلين السابقين.
- يحتاج ترحيل روابط قديمة إلى مراجع.
- يحتاج Cloud Function واختبارات Emulator.
- يحتاج خطة توافق مؤقتة مع الروابط الحالية.

### الموارد المطلوبة

- جميع ملفات الحل الثاني.
- `frontend/src/admin/SiteContent.jsx`
- `frontend/src/components/admin/site-content/fields.jsx`
- `frontend/src/services/siteContentService.js`
- مكونات الموقع التي تقرأ الصور.
- Cloud Function جديدة أو وحدة داخل Functions الحالية.
- `frontend/firestore.rules`
- وربما `frontend/storage.rules` لمنع الحذف المباشر بعد نقل العملية إلى الخادم.
- اختبارات Functions وFirestore وStorage.
- سكربت backfill واكتشاف مراجع.

## مقارنة الحلول

| المعيار | تحسين الواجهة | فصل الحقول | حماية المراجع |
|---|---:|---:|---:|
| تقليل التشوش | جيد | ممتاز | ممتاز |
| منع نشر عرضي في الجاليريا | جزئي | نعم | نعم |
| منع حذف صورة مستخدمة | لا | لا | نعم |
| معالجة الاتساق | محدود | متوسط | الأفضل |
| يحتاج ترحيل بيانات | لا | نعم | نعم |
| يحتاج Functions | لا | لا غالبًا | نعم |
| حجم التغيير | صغير | متوسط | متوسط إلى كبير |
| الأمان طويل المدى | محدود | جيد | ممتاز |

## التوصية

الحل الموصى به هو الحل الثالث، لكن على مرحلتين لتقليل المخاطر:

### المرحلة الأولى

- إضافة `showInGallery` و`usageType`.
- تنفيذ backfill.
- فصل زر النشر عن زر الإضافة للجاليريا.
- تحسين شارات الحالة والتأكيدات.
- إضافة pagination.
- تحديث قواعد Firestore لتفرض:
  - أنواع الحقول.
  - `showInGallery => isPublic`.
  - بقاء الكتابة للـAdmin فقط.

### المرحلة الثانية

- استبدال روابط الصور في محتوى الموقع بمراجع `imageId`.
- بناء `usageRefs` أو فحص مرجعي مركزي.
- نقل الحذف وتغيير الخصوصية الحساسة إلى Cloud Function.
- منع الحذف أو الإخفاء عند وجود استخدام عام.
- إضافة soft-delete وسجل تدقيق.

هذه الخطة تعالج التشوش بسرعة، ثم تغلق خطر كسر الموقع أو فقد الاتساق دون تنفيذ تغيير ضخم دفعة واحدة.

## التغييرات المطلوبة عند التنفيذ

### واجهة Hosting

- `frontend/src/admin/Media.jsx`
- `frontend/src/admin/SiteContent.jsx`
- `frontend/src/components/admin/site-content/fields.jsx`
- `frontend/src/components/public/GallerySection.jsx`
- `frontend/src/pages/PublicGallery.jsx`
- مكونات الأقسام العامة التي تقرأ روابط الصور.

### الخدمات والنموذج

- `frontend/src/services/imagesService.js`
- `frontend/src/services/imageStoragePolicy.js`
- `frontend/src/services/siteContentService.js`
- `frontend/src/utils/categorySettings.js`

### Firebase

- `frontend/firestore.rules`
- `frontend/firestore.indexes.json`
- `frontend/storage.rules` فقط إذا أصبحت العمليات الحساسة خادمية حصريًا.
- Cloud Function للحذف وتغيير الخصوصية المحميين في الحل الثالث.

### الاختبارات

- منع المتطوع من create/update/delete لوثائق الصور.
- منع anonymous من الكتابة.
- التحقق من `showInGallery => isPublic`.
- صورة عامة غير معروضة في الجاليريا.
- صورة معروضة في الجاليريا.
- منع حذف صورة مستخدمة.
- منع تحويل صورة مستخدمة في قسم عام إلى خاصة.
- نجاح حذف صورة غير مستخدمة.
- فشل Storage دون فقد وثيقة Firestore.
- فشل Firestore دون ترك ملف يتيم.
- ترحيل الصور القديمة.

## هل يلزم تعديل القواعد أو البيانات؟

### الحل الأول

- Firestore Rules: لا.
- Storage Rules: لا.
- البيانات الحالية: لا.
- Hosting: نعم.

### الحل الثاني

- Firestore Rules: نعم، لإضافة invariants والتحقق من الحقول.
- Storage Rules: لا غالبًا.
- البيانات الحالية: نعم، backfill لـ`showInGallery` و`usageType`.
- Firestore indexes: نعم على الأرجح.
- Hosting: نعم.

### الحل الثالث

- Firestore Rules: نعم.
- Storage Rules: محتمل/موصى به إذا مُنعت العمليات المباشرة.
- Functions: نعم.
- البيانات الحالية: نعم، لتحويل الروابط أو بناء المراجع.
- Hosting: نعم.

## حدود التدقيق

هذا التقرير يثبت سلوك الكود والقواعد المحلية الحالية ومخاطر التصميم.

لم يتم:

- تعديل أي ملف من ملفات التطبيق أو القواعد.
- تنفيذ Deploy.
- تنفيذ كتابة على Production.
- تشغيل ترحيل.
- فحص جميع وثائق `images` الخاصة في Production.

لذلك وجود حالات عدم اتساق فعلية في Production يحتاج لاحقًا أداة read-only مخصصة تفحص:

- `isPublic` مقابل `storagePath`.
- وجود الملف المشار إليه.
- الروابط العامة المحفوظة في صور خاصة.
- روابط الصور داخل `siteContent`.
- الملفات التي لا تملك وثيقة.
- الوثائق التي لا تملك ملفًا.

ينبغي تشغيل هذا الفحص أولًا بوضع قراءة فقط وإخراج تقرير، قبل أي backfill أو إصلاح للبيانات.
